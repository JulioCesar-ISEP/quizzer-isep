# Visão de Arquitetura

## Hexagonal (Ports & Adapters)

```text
         ┌─────────────────────────────────────┐
         │           adapters/frontend          │
         │  (React, shell, componentes)         │
         └─────────────────┬───────────────────┘
                           │
         ┌─────────────────▼───────────────────┐
         │              ports/                │
         │  survival/contracts, persistence…    │
         └─────────────────┬───────────────────┘
                           │
         ┌─────────────────▼───────────────────┐
         │              core/                 │
         │  domínio puro (regras, entidades)    │
         └─────────────────┬───────────────────┘
                           │
    ┌──────────────────────┼──────────────────────┐
    │                      │                      │
adapters/content      adapters/persistence   (futuro) RAG API
(static → RAG)        (localStorage)
```

## Frontend atual

- **Shell:** `AprendiFluxoShell` — navegação e orquestração
- **Motor Survival:** `useSurvivalEngine` — estado do exame sem UI
- **Contrato:** `SurvivalSessionPayload` + `SubjectType`

## Backend (planeado)

FastAPI + Ollama + ChromaDB + pipeline PDF imutável.
